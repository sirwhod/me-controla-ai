rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // All workspace files are served by authenticated backend routes.
    match /banks/{workspaceId}/{allPaths=**} {
      allow read, write: if false;
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
