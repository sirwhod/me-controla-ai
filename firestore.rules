rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // This application is backend-only. Firebase Admin bypasses these rules and
    // every operation is authorized in Next.js. Direct client access is denied.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
